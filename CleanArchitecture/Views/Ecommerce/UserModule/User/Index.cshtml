@{
    Layout = "Layout";
    ViewData["Title"] = "User Management";
}

<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2 mt-2">
            <div class="col-sm-6">
                <p>User Management</p>
            </div>
        </div>
    </div>
</div>

<!-- Main content -->
<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <button type="button" class="btn btn-primary mb-3 float-end"
                   data-content="@Url.Action("CreateUserModal", "User", new { area = "UserModule" })"
                    data-bs-target="#largeModal"
                    data-bs-toggle="modal"
                    style="cursor: pointer;">
                    Create
                </button>
            </div>
        </div>

        <!-- Search widget -->
        <div class="row">
            <div class="col-12">
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body table-responsive">
                        <div id="example1_wrapper" class="dataTables_wrapper dt-bootstrap4">
                            <div class="row">
                                <div class="col-md-12">
                                    <table id="dataGrid" class="table table-bordered table-striped dataTable dtr-inline" style="width: 100%" aria-describedby="example1_info">
                                        <thead>
                                            <tr>
                                                <th>Username</th>
                                                <th>FullName</th>
                                                <th>Phone</th>
                                                <th>Email</th>
                                                <th>Role</th>
                                                <th>Created At</th>
                                                <th>Action</th>
                                            </tr>
                                        </thead>
                                        <tbody class="tbody-user">
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {

    <script>
        $(function () {
              
            $('#dataGrid').DataTable({
                processing: true,
                serverSide: true,
                pagination: 10,
                ajax: {
                    url: "@Url.Action("Data", "User", new { area = "UserModule" })",
                    data: function (data) {
                        // data.business_unit_id = $('#search_business_unit_id').val();
                    }
                },
                order: [
                    [0, 'Desc']
                ],
                columns: [
                    {
                        data: 'username',
                        name: 'username',
                        orderable: false,
                        searchable: true,

                    },
                    {
                        data: 'fullName',
                        name: 'fullName',
                        orderable: false,
                        searchable: true,

                    },
                    {
                        data: 'phone',
                        name: 'phone',
                        orderable: false,
                        searchable: true,
                    },
                    {
                        data: 'email',
                        name: 'email',
                        orderable: false,
                        searchable: true,
                    },
                    {
                        data: 'roleId',
                        render: function (data, type, row) {
                            return `${row.role?.name ?? "N/A"}`;
                        }
                    },
                    {
                        name: 'createdAt',
                        data: 'createdAt',
                        orderable: false,
                        searchable: false
                    },
                    {
                        data: 'action',
                        render: function (data, type, row) {
                            let editModalURL = '@Url.Action("EditUserModal", "User", new { area = "UserModule", id = "__id__" })';
                            editModalURL = editModalURL.replace('__id__', row.id);

                            return `
                                <button type="button" class="btn btn-info float-end"
                                   data-content="${editModalURL}"
                                    data-bs-target="#largeModal"
                                    data-bs-toggle="modal"
                                    style="cursor: pointer;">
                                    <i class="fas fa-edit"></i>
                                </button>
                            `;
                        }
                    },
                ]
            });
        });
    </script>
}